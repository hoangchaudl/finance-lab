Redeploy the existing `telegram-webhook` Supabase Edge Function from the project source code to ensure the latest version is live. After deployment, confirm the function is reachable and its `verify_jwt` setting remains `false`.